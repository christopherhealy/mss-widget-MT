.mss-config-body {
  margin: 0;
  min-height: 100vh;
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
    sans-serif;
  background: radial-gradient(circle at top, #e5edff 0, #f3f4f6 40%, #e5e7eb 100%);
  color: #111827;
}

.mss-config-shell {
  max-width: 1120px;
  margin: 0 auto;
  padding: 24px 16px 40px;
}

.mss-config-header {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-bottom: 20px;
}

.mss-config-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.mss-config-mark {
  width: 32px;
  height: 32px;
  border-radius: 999px;
  background: #2563eb;
  color: #fff;
  font-weight: 700;
  font-size: 0.9rem;
  display: flex;
  align-items: center;
  justify-content: center;
}

.mss-config-brand-main {
  font-size: 1.05rem;
  font-weight: 700;
}

.mss-config-brand-sub {
  font-size: 0.85rem;
  color: #6b7280;
}

.mss-config-nav {
  display: inline-flex;
  align-items: center;
  gap: 10px;
  padding: 4px;
  border-radius: 999px;
  background: rgba(255, 255, 255, 0.8);
  box-shadow: 0 10px 25px rgba(15, 23, 42, 0.1);
}

.mss-config-nav-link {
  padding: 6px 14px;
  border-radius: 999px;
  font-size: 0.85rem;
  color: #4b5563;
  text-decoration: none;
  transition: background 0.15s ease, color 0.15s ease;
}

.mss-config-nav-link.active,
.mss-config-nav-link:hover {
  background: #2563eb;
  color: #fff;
}

.mss-config-main {
  display: block;
}

.mss-config-columns {
  display: grid;
  grid-template-columns: minmax(0, 2.2fr) minmax(0, 1.4fr);
  gap: 16px;
}

.mss-config-card {
  background: #fff;
  border-radius: 18px;
  padding: 18px 18px 16px;
  box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
}

.mss-config-card-title {
  font-size: 0.98rem;
  font-weight: 600;
  margin: 0 0 4px;
}

.mss-config-card-sub {
  margin: 0 0 12px;
  font-size: 0.84rem;
  color: #6b7280;
}

.mss-config-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 12px;
}

.mss-config-field label {
  font-size: 0.85rem;
  font-weight: 500;
}

.mss-config-field input,
.mss-config-field select {
  border-radius: 999px;
  border: 1px solid #d1d5db;
  padding: 6px 12px;
  font-size: 0.88rem;
  outline: none;
}

.mss-config-field input:focus,
.mss-config-field select:focus {
  border-color: #2563eb;
  box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.3);
}

.mss-config-grid {
  display: grid;
  gap: 8px 12px;
}

.mss-config-grid-2 {
  grid-template-columns: repeat(auto-fit, minmax(160px, 1fr));
}

.mss-config-check {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  font-size: 0.84rem;
  color: #374151;
}

.mss-config-check input[type="checkbox"] {
  width: 14px;
  height: 14px;
}

.mss-config-divider {
  height: 1px;
  background: #e5e7eb;
  margin: 10px 0 8px;
}

.mss-config-actions {
  margin-top: 12px;
}

.mss-config-saveBtn {
  border-radius: 999px;
  border: 1px solid transparent;
  background: #2563eb;
  color: #fff;
  font-size: 0.9rem;
  font-weight: 500;
  padding: 8px 18px;
  cursor: pointer;
  box-shadow: 0 16px 30px rgba(37, 99, 235, 0.4);
  transition: background 0.15s ease, transform 0.08s ease,
    box-shadow 0.12s ease;
}

.mss-config-saveBtn:hover {
  background: #1d4ed8;
  box-shadow: 0 18px 36px rgba(37, 99, 235, 0.5);
  transform: translateY(-1px);
}

.mss-config-saveBtn:disabled {
  opacity: 0.7;
  cursor: default;
  box-shadow: none;
  transform: none;
}

.mss-config-status {
  margin-top: 6px;
  font-size: 0.82rem;
  color: #6b7280;
}

.mss-config-status.ok {
  color: #15803d;
}

.mss-config-status.err {
  color: #b91c1c;
}

.mss-config-preview-card {
  height: 100%;
}

.mss-config-preview-frame-wrap {
  margin-top: 8px;
  border-radius: 14px;
  overflow: hidden;
  border: 1px solid #e5e7eb;
  background: #111827;
}

.mss-config-preview-frame {
  width: 100%;
  height: 480px;
  border: 0;
  background: transparent;
}

.mss-config-hint {
  margin-top: 8px;
  font-size: 0.78rem;
  color: #6b7280;
}

@media (max-width: 900px) {
  .mss-config-columns {
    grid-template-columns: minmax(0, 1fr);
  }

  .mss-config-preview-frame {
    height: 420px;
  }
}

@media (max-width: 640px) {
  .mss-config-shell {
    padding-inline: 12px;
  }

  .mss-config-header {
    align-items: flex-start;
  }

  .mss-config-nav {
    width: 100%;
    justify-content: center;
  }
}