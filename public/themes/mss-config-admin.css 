/* public/themes/mss-config-admin.css */

.mss-admin-body {
  font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text",
    sans-serif;
  background: radial-gradient(circle at top left, #eaf1ff 0, #f6f7fb 40%, #f8fafc 100%);
  margin: 0;
  color: #111827;
}

.mss-admin-shell {
  max-width: 1120px;
  margin: 32px auto 40px;
  padding: 0 16px;
}

/* Header & nav */

.mss-admin-header {
  display: flex;
  flex-direction: column;
  gap: 16px;
  margin-bottom: 24px;
}

.mss-admin-brand {
  display: flex;
  align-items: center;
  gap: 12px;
}

.mss-admin-logo {
  width: 40px;
  height: 40px;
  border-radius: 999px;
  background: #111827;
  color: #f9fafb;
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: 14px;
}

.mss-admin-titleblock {
  display: flex;
  flex-direction: column;
  gap: 2px;
}

.mss-admin-title {
  font-weight: 600;
  font-size: 18px;
}

.mss-admin-subtitle {
  font-size: 13px;
  color: #6b7280;
}

.mss-admin-school {
  font-size: 13px;
  color: #4b5563;
}

.mss-admin-nav {
  display: inline-flex;
  gap: 16px;
  align-items: center;
}

.mss-admin-nav-link {
  font-size: 14px;
  color: #4b5563;
  text-decoration: none;
  padding-bottom: 4px;
  border-bottom: 2px solid transparent;
}

.mss-admin-nav-link.is-active {
  color: #111827;
  font-weight: 500;
  border-bottom-color: #2563eb;
}

.mss-admin-nav-link.is-disabled {
  opacity: 0.4;
  pointer-events: none;
}

/* Layout */

.mss-admin-main {
  display: grid;
  grid-template-columns: minmax(0, 2.1fr) minmax(0, 1.5fr);
  gap: 24px;
}

@media (max-width: 960px) {
  .mss-admin-main {
    grid-template-columns: minmax(0, 1fr);
  }
}

/* Cards */

.mss-admin-card {
  background: #ffffff;
  border-radius: 20px;
  box-shadow: 0 18px 40px rgba(15, 23, 42, 0.08);
  padding: 24px 24px 20px;
}

.mss-admin-card-title {
  margin: 0 0 4px;
  font-size: 18px;
  font-weight: 600;
}

.mss-admin-card-intro {
  margin: 0 0 12px;
  font-size: 14px;
  color: #4b5563;
}

.mss-admin-divider {
  border: 0;
  border-top: 1px solid #e5e7eb;
  margin: 18px 0;
}

/* Sections & fields */

.mss-admin-section {
  margin-bottom: 12px;
}

.mss-admin-section-title {
  font-size: 15px;
  font-weight: 600;
  margin: 0 0 12px;
}

.mss-admin-subsection {
  margin-top: 10px;
}

.mss-admin-subsection-title {
  margin: 0 0 8px;
  font-size: 14px;
  font-weight: 500;
  color: #374151;
}

.mss-admin-field {
  display: flex;
  flex-direction: column;
  gap: 4px;
  margin-bottom: 10px;
  font-size: 14px;
}

.mss-admin-field span {
  color: #374151;
}

.mss-admin-field input,
.mss-admin-field select {
  border-radius: 10px;
  border: 1px solid #d1d5db;
  padding: 8px 10px;
  font-size: 14px;
  outline: none;
}

.mss-admin-field input:focus,
.mss-admin-field select:focus {
  border-color: #2563eb;
  box-shadow: 0 0 0 1px rgba(37, 99, 235, 0.12);
}

.mss-admin-inline-fields {
  display: flex;
  gap: 12px;
  flex-wrap: wrap;
}

.mss-admin-field-inline {
  flex: 1 1 140px;
}

.mss-admin-checkbox-row {
  margin: 6px 0;
  font-size: 14px;
}

.mss-admin-checkbox-row label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
}

.mss-admin-checkbox-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
  gap: 6px 16px;
  font-size: 13px;
}

.mss-admin-checkbox-grid label {
  display: inline-flex;
  align-items: center;
  gap: 6px;
  cursor: pointer;
}

.mss-admin-label-row input {
  font-size: 13px;
}

/* Actions & status */

.mss-admin-actions {
  margin-top: 16px;
  display: flex;
  align-items: center;
  gap: 12px;
}

.mss-admin-primary-btn {
  border: none;
  border-radius: 999px;
  padding: 10px 22px;
  font-size: 14px;
  font-weight: 500;
  background: #2563eb;
  color: #f9fafb;
  cursor: pointer;
  box-shadow: 0 14px 30px rgba(37, 99, 235, 0.38);
}

.mss-admin-primary-btn:hover {
  background: #1d4ed8;
}

.mss-admin-status {
  font-size: 13px;
  color: #6b7280;
}

.mss-admin-status.is-working {
  color: #2563eb;
}

.mss-admin-status.is-ok {
  color: #15803d;
}

.mss-admin-status.is-error {
  color: #b91c1c;
}

/* Preview */

.mss-admin-preview-card {
  height: 100%;
}

.mss-admin-preview-frame {
  margin-top: 10px;
  border-radius: 18px;
  overflow: hidden;
  border: 1px solid #e5e7eb;
  background: #f9fafb;
  min-height: 360px;
}

#mssWidgetPreview {
  width: 100%;
  height: 420px;
  border: none;
}

.mss-admin-preview-note {
  margin-top: 10px;
  font-size: 13px;
  color: #6b7280;
}

/* -------- Preview overlay -------- */

.mss-admin-overlay {
  position: fixed;
  inset: 0;
  z-index: 999;
}

.mss-admin-overlay-backdrop {
  position: absolute;
  inset: 0;
  background: rgba(15, 23, 42, 0.45);
  backdrop-filter: blur(3px);
}

.mss-admin-overlay-inner {
  position: absolute;
  inset: 32px;
  margin: auto;
  max-width: 960px;
  max-height: 620px;
  background: #ffffff;
  border-radius: 18px;
  box-shadow: 0 18px 45px rgba(15, 23, 42, 0.25);
  display: flex;
  flex-direction: column;
  overflow: hidden;
}

.mss-admin-overlay-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 12px 16px;
  border-bottom: 1px solid #e5e7eb;
}

.mss-admin-overlay-title {
  font-weight: 600;
  font-size: 14px;
}

.mss-admin-overlay-close {
  border: none;
  background: transparent;
  font-size: 20px;
  line-height: 1;
  padding: 4px 8px;
  cursor: pointer;
  color: #6b7280;
}

.mss-admin-overlay-frame {
  border: 0;
  width: 100%;
  flex: 1 1 auto;
}

/* We already use .mss-hidden elsewhere */
.mss-hidden {
  display: none !important;
}