{
  "version": 2,
  "builds": [
    { "src": "public/**", "use": "@vercel/static" }
  ],
  "routes": [
    { "src": "^/api/(.*)$", "dest": "https://api.eslsuccess.club/api/$1" },

    { "handle": "filesystem" },

    { "src": "^(?!/api/).*$", "dest": "/index.html" }
  ]
}